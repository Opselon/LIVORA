# R1 lane requests — Engines/Verification repair (wave4/p1r/r1-engines)

Format: `R-r1-<n> | target file | what you need | why | blocking`

R-r1-1 | server/src/Livora.Server/appsettings*.json | flip `Modules:Intelligence:SchemaContribution` to `true` (or state why not) now that the single Wave4P1Schema migration exists | the engines ledger/pattern endpoints still answer the documented 503 degradation while the gate is off; persistence itself is proven green in EnginePersistenceTests against real SQLite | no

R-r1-2 | docs/contracts/wave4/SERVER-CONTRACT-P1.md | record the pinned budget split: ≤3 visible ASKS, ≤4 visible cards total; zero-minute plan adjustments (bedtime shift, intensity cap) do not consume the ask budget; selection order = priority desc then score desc (client RecommendationService.cs:27-33 parity) | CrossDomainPipelineTests + PrioritisationScheduleFeedbackTests pin these numbers; the contract text only says "1-3 recommendations" | no

R-r1-3 | Resources/Localization/AppResources*.resx (FROZEN for this lane) | wording divergence, same key: client `Rec.CompleteHabit` = "Do the small version of a skipped habit"; the server explanation table (DeterministicExplanationRenderer.cs:44) = "Complete '{0}' to keep the {1}-day streak." The server ships its own en+fa templates so no new client keys are required — lead decides which prose wins | disclosed, not blocking | no

R-r1-4 | lead verification | `EnginesSchemaGate.EnginesSchemaStatus` (new DI singleton, per-host) replaces the process-wide `EnginesSchemaGate.Active` static read in handlers | the static leaked across test hosts in one process: a host with the gate ON made a later gate-OFF host answer 500 "no such table" where the honest degradation is 503 | no
