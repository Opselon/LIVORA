# Wave 4 R3 (quality repair lane) — requests to lanes/lead

Format: `R-R3-<n> | owner | what you need | why (evidence) | blocking?`
Everything below was FOUND BY EXECUTION at HEAD `1c7bd50`+ (commits bc32b2f…1a879cb) in this
worktree. This lane may not edit other lanes' files, so each is a request line, not a fix.
The failing-test evidence is the merged-server-suite log (381 tests: 353 passed / 28 failed,
2026-09-14 ~21:15 local; command `dotnet test server/tests/Livora.Server.Tests/Livora.Server.Tests.csproj --nologo`),
and every Quality/** test is green at this HEAD (75/75, 3 m 20 s).

## Findings from the re-run honesty tripwire set + full suite (genuine violations in OTHER lanes' code)

R-R3-1 | P1-D (w4-p1d-client-seam) | repair `wave4-keys/lane-p1d.en.keys.xml:15,61,75,83,86,120` and
`lane-p1d.fa.keys.xml:8,54,68,76,79,113`: XML comments contain `----` (`<!-- ---- §5c … ---- -->`),
which is INVALID per XML 1.0 §2.5 — a strict parser throws | Evidence: `System.Xml.XmlException : An
XML comment cannot contain '--' … Line 15, position 8` (python minidom agrees on both files; the
other 20 wave3b/wave3c manifests parse fine). The quality lane certifies these manifests through a
documented comment-only normalisation (TripwireScanner.ReadKeys) so the bilingual EN/FA law is
enforced on the real data bytes (100 keys × 2, sets identical, no placeholder drift, FA all Persian
— verified), but shipping manifests must be valid XML: the resx merge driver and any future tooling
will hit the same throw. The T4 test prints `T4 MANIFEST HEALTH: MALFORMED …` until fixed | blocking? no (certification works around it, disclosed)

R-R3-2 | P1-D | remove or honour the false test claim in the lane-p1d manifest headers: both files
say `Tests/Tests/Wave4ClientSeam/CloudKeyManifestTests.cs enforces that pair` — that file does not
exist anywhere in the tree (`find Tests -iname '*CloudKeyManifest*'` → empty; `Tests/Tests/Wave4ClientSeam/`
holds six other files) | why: CONTRACT-P1 §7 bans claims without the machinery behind them; a
comment pointing at a nonexistent gate is exactly a compliance lie by omission. Either land the
test or cite a real one (the server-side PlantedViolationTests.T4 DOES certify the pair today) | blocking? no

R-R3-3 | P1-E (engines) | 25 failing tests in `server/tests/Livora.Server.Tests/Engines/**` at the
merged HEAD — the suite shipped red | exact list (full-suite log 2026-09-14):
BaselineStatePatternTests ×4 (window-count off-by-ones: expected 30 got 29, expected 4 got 3,
Days7→null, midnight wrap 40 vs 1425-1485 ⇒ UTC-ms convention conversion missed in this lane's
fixtures/tests), CrossDomainPipelineTests ×8 (empty/oversized plan collections, two
ArgumentOutOfRange), PrioritisationScheduleFeedbackTests ×8 (float-equality + weights sum 1.08≠1 —
the lane's own W-weights contract), WeeklyAndExplanationTests ×2 (`[missing:Rec.CompleteHabit]` —
the English template dictionary lacks the key), EnginePersistenceTests ×1 + EnginesApiTests ×1
(`The LINQ expression 'DbSet<VerificationClaimRecord>() … OrderByDescending(CreatedatUtc).ThenBy(c => c.Id)'
could not be translated` → 500 where the test expects the schema-gate 503; the query needs a
`CreatedAtUtc`-tiebreak rewrite EF can translate, e.g. order by the stored UTC-ms column),
EngineStateTests ×1 (freshness 99 vs 42 — same clock-convention family), FusionEngineTests ×1.
Not one of them is in Quality/**; the gate harness enumerates them as foreign findings (documented
self-reference policy in Wave4GateTests) | blocking? YES for the release gate's server-tests row (it
is the only thing keeping the merged suite red besides R-R3-4/5)

R-R3-4 | P1-B (w4-p1b-platform/sync) | `SyncApiContractTests.Sync_endpoints_are_visible_in_the_openapi_document`
fails: the OpenAPI document has NO `Idempotency-Key` parameter for POST /api/v1/sync/batch
(`paths["/api/v1/sync/batch"].post.parameters` is absent — verified against the live document JSON)
| the route reads the header manually, so add `.WithOpenApi(o => o with { Parameters = … })` or the
`[FromHeader]` shape the SDK advertises — otherwise §5c clients have no machine-readable contract
for the mandatory header | blocking? yes (the lane's own test is red; the header IS enforced at runtime —
SecurityChecklistTests.S4 proves the 400 refusal)

R-R3-5 | P1-B | `SyncApiContractTests.A_rebased_retry_after_a_conflict_can_apply` throws
`JsonReaderException: 'm' is an invalid start of a value` — the test's own line
`Body(("op-3", "goal", entity, "update", 1, "{\"t\":mine}"), …)` puts raw `mine` (unquoted) in a
JSON payload at SyncApiContractTests.cs:419 | fix the fixture string to `"{\"t\":\"mine\"}"` (the
server rejecting malformed payload JSON is CORRECT behaviour; the test asserts the wrong thing about
per-op rejection anyway by EnsureSuccessStatusCode on a 200-with-rejected — clarify which) | blocking? yes (lane's own test red)

R-R3-6 | P1-C (identity) | the IdentityService header claims "INVARIANTS (each is pinned by a test
in tests/…/Identity/)" — there is NO Identity test directory (`ls server/tests/Livora.Server.Tests/`
→ Engines, Fixtures, Fixtures_Wave4, Persistence, Platform, Quality, Sync, Verification) and no
other test file references `/api/v1/auth/*` besides this lane's Quality probes | §7 again: a claimed
test surface that does not exist. Either land the lane's Identity tests (register/login/refresh
rotation+theft, lockout ladder, deletion lifecycle are all implemented and reachable — this lane's
S2a/S2b prove session-IDOR + self-scoped export through the real API today) or delete the claim |
blocking? no (quality lane covers the §5c auth-behaviour rows S1/S2a/S2b/S2c; the lane's own
invariant suite is what is missing)

R-R3-7 | lead / integration | `docs/quality/wave4/PERF-BUDGETS.md` + `Wave4TruthMatrix.md` rows 1–3
were stale against the merged tree; this lane updated both from executed runs, but the matrix's
feature-owner rows (4–13) still show pre-P1 states for intelligence/verification (modules + engines
now ship with endpoints — the capability report at boot lists `identity,intelligence,platform,sync,verification`) |
owners should refresh their own rows at P2 start | blocking? no

R-R3-8 | lead | apply `CI-SERVER-JOB.md` unchanged (R-P1F-1 restated): the two-step client split and
the server job are what make the perf-budget contention policy (Wave4PerformanceBudgetTests header)
and the gate-harness self-reference policy unnecessary in CI; locally, the sync/batch write path
measures 5–8x slower under the full-suite disk queue (p95 ~56 ms serial vs ~235-630 ms in-suite),
so the serial enforcement step is load-bearing, not cosmetic | blocking? no (harness discloses it)

## What R3's own scanner widenings were (cross-reference — full text in TRIPWIRES.md)

- Tripwire 1: `.Result` now requires Task-shaped receiver evidence (`.Wait()`/`.GetAwaiter().GetResult()`
  unchanged, unconditional) — the merged verification DTOs (`RuleOutcome(…, string Result, …)`) were
  honest property reads.
- Tripwire 4: a bare-identifier code argument is excused ONLY when every same-file assignment to it
  is provably ProblemCodes/nameof constants (P1-B's `replayCode` ternary); unproven = fires.
- T2 (word-level): state-machine evidence now accepts an in-file `enum *Status|State|Grade|…`
  declaration (P1-E's Verification.cs defines VerificationStatus + EvidenceGrades and exists
  precisely to refuse a collapsed "verified" — the rule was flagging its own antidote).
- T4 (manifests): comment-only XML normalisation, strict-parse-first, defect counted + printed +
  filed above (R-R3-1).
